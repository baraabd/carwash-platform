# Context for: docker build -f apps/api-gateway/Dockerfile .
# Dockerfile-specific rules replace the root .dockerignore. Keep the root
# service-image policy unchanged, and include only the gateway application.
**/node_modules
**/dist
**/dist-tests
**/src/generated
.git
.github
.acceptance
evidence
design
docs
apps/*
!apps/api-gateway
tests
infra
scripts/dev
*.md
.env
.env.*
**/.env
**/.env.*
!.env.example
