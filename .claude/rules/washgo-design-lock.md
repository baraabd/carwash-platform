---
paths:
  - "apps/customer-web/**"
  - "apps/operator-web/**"
  - "apps/admin-web/**"
  - "packages/ui/**"
  - "design/**"
  - "docs/design/**"
---

# Frozen approved design
Read `/docs/design/DESIGN_LOCK.md` and `/AGENTS.md` before editing these paths.
Recreate the final payments HTML without visual or interaction drift.
No step merging/reordering, new theme, icon replacement, automatic next-on-select,
mandatory plate, removed before/after, or hidden payment method.
No regeneration of reference files/hashes/snapshots to pass a check.
Maintain Arabic RTL, mobile shell, original SVGs, motion + reduced-motion, accessible focus.
Changing a library or introducing React is not permission to change the appearance.
Explicit owner approval is required for a visible design change.
Unimplemented admin/operator pages are not magically preapproved by these rules.
